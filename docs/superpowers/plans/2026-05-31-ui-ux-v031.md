# UI/UX v0.31.0 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corrigir 5 problemas visuais identificados em v0.30.9 — contraste modo claro em QueuePage e HelpModal, texto URL no preview, pipeline com progresso em tempo real no histórico de jobs, versão completa no sidebar.

**Architecture:** Alterações cirúrgicas em 5 ficheiros existentes + 1 ficheiro novo (`src/lib/pipeline.ts`) que extrai constantes partilhadas entre QueuePage e AssetDetailPage. Sem novos componentes. Sem novas dependências.

**Tech Stack:** React 19, TypeScript, Tailwind CSS v4, i18next

---

## File Map

| Ficheiro                        | Operação  | O que muda                                                                                                         |
| ------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------ |
| `src/lib/pipeline.ts`           | Criar     | Extrair PIPELINE_STEPS, PIPELINE_PHASES, getStepIndex                                                              |
| `src/pages/QueuePage.tsx`       | Modificar | Importar de pipeline.ts; `bg-bg-primary` → `bg-bg-tertiary dark:bg-bg-primary` (2 locais)                          |
| `src/components/HelpModal.tsx`  | Modificar | Sidebar inactivo `text-text-muted` → `text-text-secondary`; ChevronRight `/60` → `/80`; Tips label                 |
| `src/pages/AssetDetailPage.tsx` | Modificar | Overlay URL → `text-white/90`; path text → `text-text-secondary`; substituir mini-pipeline por fases com progresso |
| `src/App.tsx`                   | Modificar | Substituir `v{appVersion}` por `APP_VERSION` de `@/lib/version`; remover state e useEffect do getVersion           |

---

## Task 1: Criar src/lib/pipeline.ts

**Ficheiros:**

- Create: `src/lib/pipeline.ts`

Extrair as constantes de pipeline que existem duplicadas em `QueuePage.tsx` (linhas 57-72) e `AssetDetailPage.tsx` (linhas 96-105) para um ficheiro partilhado.

**Dados exactos do código actual:**

- QueuePage tem `{ key: string }` sem `short`
- AssetDetailPage tem `{ key: string, short: string }` com abreviaturas
- Ambos usam hífens: `qc-pre`, `qc-post` (não underscores)
- Fases usam chaves i18n existentes: `queue.phaseAnalyse`, `queue.phaseConvert`, `queue.phaseVerify`

- [ ] **Step 1: Criar `src/lib/pipeline.ts` com o seguinte conteúdo exacto**

```typescript
export const PIPELINE_STEPS = [
  { key: 'ingest', short: 'IN' },
  { key: 'qc-pre', short: 'QC' },
  { key: 'transcode', short: 'TR' },
  { key: 'audio', short: 'AU' },
  { key: 'proxy', short: 'PX' },
  { key: 'thumbnail', short: 'TH' },
  { key: 'qc-post', short: 'QP' },
  { key: 'delivery', short: 'DL' },
] as const;

export type PipelineStepKey = (typeof PIPELINE_STEPS)[number]['key'];

export const PIPELINE_PHASES = [
  { labelKey: 'queue.phaseAnalyse', steps: ['ingest', 'qc-pre'] as PipelineStepKey[] },
  {
    labelKey: 'queue.phaseConvert',
    steps: ['transcode', 'audio', 'proxy', 'thumbnail'] as PipelineStepKey[],
  },
  { labelKey: 'queue.phaseVerify', steps: ['qc-post', 'delivery'] as PipelineStepKey[] },
];

export function getStepIndex(stepKey: string | null): number {
  if (!stepKey) return -1;
  return PIPELINE_STEPS.findIndex((s) => s.key === stepKey);
}
```

- [ ] **Step 2: Verificar TypeScript**

```bash
npm run typecheck 2>&1 | tail -5
```

Esperado: `tsc --noEmit` sem erros.

- [ ] **Step 3: Commit**

```bash
git add src/lib/pipeline.ts
git commit -m "feat(lib): extrair PIPELINE_STEPS, PIPELINE_PHASES, getStepIndex para src/lib/pipeline.ts"
```

---

## Task 2: QueuePage — importar pipeline.ts + fix light mode

**Ficheiros:**

- Modify: `src/pages/QueuePage.tsx:57-72` (remover constantes locais)
- Modify: `src/pages/QueuePage.tsx:204-207` (remover getStepIndex local)
- Modify: `src/pages/QueuePage.tsx:315` (fix Film container light mode)
- Modify: `src/pages/QueuePage.tsx:358` (fix fase inactiva light mode)

- [ ] **Step 1: Adicionar import de pipeline.ts no topo de QueuePage.tsx**

Localizar os imports no topo do ficheiro. Adicionar após os últimos imports de `@/`:

```tsx
import { PIPELINE_STEPS, PIPELINE_PHASES, getStepIndex } from '@/lib/pipeline';
```

- [ ] **Step 2: Remover as constantes locais PIPELINE_STEPS e PIPELINE_PHASES (linhas 57-72)**

Apagar completamente:

```tsx
// REMOVER TUDO ISTO (linhas 57-72):
const PIPELINE_STEPS = [
  { key: 'ingest' },
  { key: 'qc-pre' },
  { key: 'transcode' },
  { key: 'audio' },
  { key: 'proxy' },
  { key: 'thumbnail' },
  { key: 'qc-post' },
  { key: 'delivery' },
];

const PIPELINE_PHASES = [
  { labelKey: 'queue.phaseAnalyse', steps: ['ingest', 'qc-pre'] },
  { labelKey: 'queue.phaseConvert', steps: ['transcode', 'audio', 'proxy', 'thumbnail'] },
  { labelKey: 'queue.phaseVerify', steps: ['qc-post', 'delivery'] },
];
```

- [ ] **Step 3: Remover a função local `getStepIndex` (linhas 204-207)**

Apagar completamente:

```tsx
// REMOVER TUDO ISTO:
const getStepIndex = (stepKey: string | null) => {
  if (!stepKey) return -1;
  return PIPELINE_STEPS.findIndex((s) => s.key === stepKey);
};
```

- [ ] **Step 4: Fix contentor do ícone Film (linha ~315, após remoção das linhas acima)**

Localizar a linha com `w-12 h-12 bg-bg-primary rounded-lg flex items-center justify-center`:

```tsx
// ANTES:
<div className="w-12 h-12 bg-bg-primary rounded-lg flex items-center justify-center">
// DEPOIS:
<div className="w-12 h-12 bg-bg-tertiary dark:bg-bg-primary rounded-lg flex items-center justify-center">
```

- [ ] **Step 5: Fix card de fase inactiva (linha ~358, dentro do map de PIPELINE_PHASES)**

Localizar `: 'border-border bg-bg-primary'` (último valor do ternário isDone/isActive):

```tsx
// ANTES:
: 'border-border bg-bg-primary'
// DEPOIS:
: 'border-border bg-bg-tertiary dark:bg-bg-primary'
```

- [ ] **Step 6: Verificar TypeScript**

```bash
npm run typecheck 2>&1 | tail -5
```

Esperado: sem erros.

- [ ] **Step 7: Commit**

```bash
git add src/pages/QueuePage.tsx
git commit -m "fix(queue): importar pipeline.ts + bg-bg-tertiary em modo claro para cards e icone Film"
```

---

## Task 3: HelpModal — contraste sidebar e tips em modo claro

**Ficheiros:**

- Modify: `src/components/HelpModal.tsx:~145` (ScreenCard tips label)
- Modify: `src/components/HelpModal.tsx:~182` (ChevronRight icon)
- Modify: `src/components/HelpModal.tsx:~415` (sidebar inactive text)

- [ ] **Step 1: Fix texto do sidebar inactivo**

Localizar o `cn()` dos botões do sidebar (procurar `text-text-muted hover:bg-bg-hover hover:text-text-secondary`):

```tsx
// ANTES:
: 'text-text-muted hover:bg-bg-hover hover:text-text-secondary border-l-2 border-transparent',
// DEPOIS:
: 'text-text-secondary hover:bg-bg-hover hover:text-text-primary border-l-2 border-transparent',
```

- [ ] **Step 2: Fix label "Tips" na função ScreenCard**

Localizar `text-[10px] font-bold text-text-muted uppercase tracking-wider mb-1.5` (dentro da função ScreenCard):

```tsx
// ANTES:
<p className="text-[10px] font-bold text-text-muted uppercase tracking-wider mb-1.5">
// DEPOIS:
<p className="text-[10px] font-bold text-text-secondary uppercase tracking-wider mb-1.5">
```

- [ ] **Step 3: Fix ChevronRight icon na função ScreenCard**

Localizar `<ChevronRight className="w-3 h-3 shrink-0 mt-0.5 text-brand/60" />`:

```tsx
// ANTES:
<ChevronRight className="w-3 h-3 shrink-0 mt-0.5 text-brand/60" />
// DEPOIS:
<ChevronRight className="w-3 h-3 shrink-0 mt-0.5 text-brand/80" />
```

- [ ] **Step 4: Verificar TypeScript**

```bash
npm run typecheck 2>&1 | tail -5
```

Esperado: sem erros.

- [ ] **Step 5: Commit**

```bash
git add src/components/HelpModal.tsx
git commit -m "fix(help-modal): contraste sidebar inactivo e tips em modo claro"
```

---

## Task 4: AssetDetailPage — URL overlay + path text + pipeline com progresso

**Ficheiros:**

- Modify: `src/pages/AssetDetailPage.tsx:96-105` (substituir PIPELINE_STEPS local pelo import)
- Modify: `src/pages/AssetDetailPage.tsx:~407` (overlay URL text → white)
- Modify: `src/pages/AssetDetailPage.tsx:~416` (path text → text-secondary)
- Modify: `src/pages/AssetDetailPage.tsx:930-960` (substituir mini-pipeline por fases com progresso)

- [ ] **Step 1: Adicionar imports de pipeline.ts e React no topo de AssetDetailPage.tsx**

Localizar os imports no topo. Adicionar:

```tsx
import { PIPELINE_STEPS, PIPELINE_PHASES, getStepIndex } from '@/lib/pipeline';
```

Verificar se `React` já está importado (necessário para `React.Fragment` e `CheckCircle2`). Se não: `import React from 'react';`

- [ ] **Step 2: Remover PIPELINE_STEPS local (linhas 96-105)**

Apagar completamente:

```tsx
// REMOVER TUDO ISTO (linhas 96-105):
const PIPELINE_STEPS = [
  { key: 'ingest', short: 'IN' },
  { key: 'qc-pre', short: 'QC' },
  { key: 'transcode', short: 'TR' },
  { key: 'audio', short: 'AU' },
  { key: 'proxy', short: 'PX' },
  { key: 'thumbnail', short: 'TH' },
  { key: 'qc-post', short: 'QP' },
  { key: 'delivery', short: 'DL' },
];
```

- [ ] **Step 3: Fix texto dentro do overlay de hover (linha ~407 após remoção acima)**

Localizar `<p className="text-[10px] font-mono text-text-secondary truncate">` (dentro do `bg-black/60 backdrop-blur-md`):

```tsx
// ANTES:
<p className="text-[10px] font-mono text-text-secondary truncate">
// DEPOIS:
<p className="text-[10px] font-mono text-white/90 truncate">
```

- [ ] **Step 4: Fix path abaixo do player (linha ~416)**

Localizar `<div className="flex items-start gap-2 text-[10px] text-text-muted font-mono leading-relaxed">` (imediatamente após o `</div>` do contentor do player):

```tsx
// ANTES:
<div className="flex items-start gap-2 text-[10px] text-text-muted font-mono leading-relaxed">
// DEPOIS:
<div className="flex items-start gap-2 text-[10px] text-text-secondary font-mono leading-relaxed">
```

- [ ] **Step 5: Substituir o bloco `{/* MINI PIPELINE */}` (linhas ~930-960)**

Localizar o bloco que começa com `{/* MINI PIPELINE */}` e vai até ao `</div>` que encerra os steps. Substituir por:

```tsx
{
  /* PIPELINE — fases com progresso para jobs activos, steps para jobs concluídos */
}
{
  job.status === 'processing' ? (
    <div className="mb-4">
      {/* FASES COMPACTAS */}
      <div className="flex items-stretch gap-2 mb-3">
        {PIPELINE_PHASES.map((phase, phaseIdx) => {
          const stepIndices = phase.steps.map((s) => getStepIndex(s));
          const phaseMin = Math.min(...stepIndices);
          const phaseMax = Math.max(...stepIndices);
          const currentIdx = getStepIndex(job.step);
          const isDone = currentIdx > phaseMax;
          const isActive = currentIdx >= phaseMin && currentIdx <= phaseMax;
          return (
            <React.Fragment key={phase.labelKey}>
              <div
                className={`flex-1 rounded-xl border p-2 flex flex-col gap-1.5 transition-all ${
                  isDone
                    ? 'border-green-500/40 bg-green-500/5'
                    : isActive
                      ? 'border-brand/60 bg-brand/5'
                      : 'border-border bg-bg-tertiary dark:bg-bg-primary'
                }`}
              >
                <div className="flex items-center gap-1.5">
                  <div
                    className={`w-3 h-3 rounded-full shrink-0 flex items-center justify-center ${
                      isDone ? 'bg-green-500' : isActive ? 'bg-brand' : 'bg-surface'
                    }`}
                  >
                    {isDone && <CheckCircle2 size={8} className="text-white" />}
                    {isActive && (
                      <div className="w-1.5 h-1.5 bg-white rounded-full animate-pulse" />
                    )}
                  </div>
                  <span
                    className={`text-[9px] font-black uppercase tracking-widest ${
                      isDone ? 'text-green-500' : isActive ? 'text-brand' : 'text-text-muted'
                    }`}
                  >
                    {t(phase.labelKey)}
                  </span>
                </div>
                <div className="flex gap-1 flex-wrap">
                  {phase.steps.map((stepKey) => {
                    const idx = getStepIndex(stepKey);
                    const currentIdx = getStepIndex(job.step);
                    const stepDone = idx < currentIdx;
                    const stepActive = idx === currentIdx;
                    return (
                      <span
                        key={stepKey}
                        className={`px-1 py-0.5 rounded text-[8px] font-bold uppercase ${
                          stepDone
                            ? 'bg-green-500/20 text-green-500'
                            : stepActive
                              ? 'bg-brand/20 text-brand'
                              : 'bg-surface text-text-muted'
                        }`}
                      >
                        {t(`pipeline.${stepKey}`, stepKey)}
                      </span>
                    );
                  })}
                </div>
              </div>
              {phaseIdx < PIPELINE_PHASES.length - 1 && (
                <div className="flex items-center mt-1">
                  <div
                    className={`w-4 h-0.5 ${
                      getStepIndex(job.step) > Math.max(...phase.steps.map((s) => getStepIndex(s)))
                        ? 'bg-green-500'
                        : 'bg-surface'
                    }`}
                  />
                </div>
              )}
            </React.Fragment>
          );
        })}
      </div>
      {/* BARRA DE PROGRESSO DO STEP ACTIVO */}
      <div className="space-y-1">
        <div className="flex justify-between items-center">
          <span className="text-[10px] font-bold text-brand">
            {job.step ?? '…'} — {(job.progress * 100).toFixed(0)}%
          </span>
          <span className="text-[10px] text-text-muted">
            {t('queue.step', { step: job.step ?? '…' })}
          </span>
        </div>
        <div className="w-full h-1.5 bg-surface rounded-full overflow-hidden">
          <div
            className="h-full bg-brand transition-all duration-500"
            style={{ width: `${(job.progress * 100).toFixed(0)}%` }}
          />
        </div>
      </div>
    </div>
  ) : (
    /* Jobs concluídos/erro — mini pipeline com steps */
    <div className="flex items-center justify-between mb-6 px-2 opacity-60">
      {PIPELINE_STEPS.map((step, idx) => {
        const isDone = job.status === 'done';
        return (
          <React.Fragment key={step.key}>
            <div className="flex flex-col items-center gap-1">
              <div
                className={`w-5 h-5 rounded-full border flex items-center justify-center ${
                  isDone
                    ? 'bg-green-500 border-green-500 text-white'
                    : 'border-border text-text-muted'
                }`}
              >
                {isDone ? (
                  <CheckCircle2 size={10} />
                ) : (
                  <span className="text-[8px] font-bold">{step.short}</span>
                )}
              </div>
            </div>
            {idx < PIPELINE_STEPS.length - 1 && (
              <div className={`flex-1 h-px mx-[-2px] ${isDone ? 'bg-green-500' : 'bg-border'}`} />
            )}
          </React.Fragment>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 6: Verificar TypeScript**

```bash
npm run typecheck 2>&1 | tail -5
```

Esperado: sem erros. Se surgir erro de tipo em `phase.steps.map((s) => getStepIndex(s))`, verificar que `PipelineStepKey` está a ser exportado de `pipeline.ts` e que o cast `as PipelineStepKey[]` está presente no ficheiro.

- [ ] **Step 7: Commit**

```bash
git add src/pages/AssetDetailPage.tsx
git commit -m "fix(asset-detail): URL overlay text-white, path text-secondary, pipeline fases com progresso para jobs activos"
```

---

## Task 5: App.tsx — versão completa no sidebar

**Ficheiros:**

- Modify: `src/App.tsx:3` (remover import getVersion)
- Modify: `src/App.tsx:54` (remover estado appVersion)
- Modify: `src/App.tsx:215-223` (remover getVersion do useEffect)
- Modify: `src/App.tsx:375` (substituir v{appVersion} por APP_VERSION)

- [ ] **Step 1: Adicionar import de APP_VERSION**

No topo do ficheiro, junto dos outros imports de `@/`:

```tsx
import { APP_VERSION } from '@/lib/version';
```

- [ ] **Step 2: Remover import de getVersion (linha 3)**

```tsx
// ANTES (linha 3):
import { getVersion } from '@tauri-apps/api/app';
// DEPOIS: apagar esta linha completamente
```

- [ ] **Step 3: Remover estado appVersion (linha 54)**

```tsx
// ANTES (linha 54):
const [appVersion, setAppVersion] = useState('—');
// DEPOIS: apagar esta linha completamente
```

- [ ] **Step 4: Remover getVersion do useEffect (linhas 215-223)**

O useEffect tem duas partes: (a) getVersion e (b) verificar pré-requisitos. Remover APENAS a parte (a). O bloco a remover é:

```tsx
// REMOVER APENAS ESTAS LINHAS do useEffect:
getVersion()
  .then(setAppVersion)
  .catch(() =>
    invoke<string>('get_app_version')
      .then(setAppVersion)
      .catch(() => setAppVersion('?')),
  );
```

O resto do useEffect (verificações de pré-requisitos a partir da linha 225) deve ficar intacto.

- [ ] **Step 5: Substituir v{appVersion} por APP_VERSION (linha 375)**

```tsx
// ANTES:
<span className="text-[10px] font-bold text-brand uppercase">v{appVersion}</span>
// DEPOIS:
<span className="text-[10px] font-bold text-brand">{APP_VERSION}</span>
```

Nota: remover `uppercase` porque APP_VERSION já tem o formato correcto (`v0.30.9-beta.1`) — `uppercase` tornaria `V0.30.9-BETA.1`.

- [ ] **Step 6: Verificar TypeScript**

```bash
npm run typecheck 2>&1 | tail -5
```

Esperado: sem erros. Se `useState` ficou sem uso, verificar se ainda é necessário noutro sítio.

- [ ] **Step 7: Commit**

```bash
git add src/App.tsx
git commit -m "fix(app): versao completa no sidebar usando APP_VERSION (inclui sufixo beta)"
```

---

## Task 6: Verificação final

- [ ] **Step 1: Correr typecheck e cargo fmt completos**

```bash
npm run typecheck 2>&1 | tail -5
cd src-tauri && cargo fmt --check 2>&1 && echo "fmt OK" && cd ..
```

Esperado: sem erros em ambos.

- [ ] **Step 2: Verificar checklist visual**

Arrancar a app em modo desenvolvimento (`npm run dev` ou equivalente não disponível em sandbox — testar no próximo release):

| #   | Verificação                                    | Esperado                                                                 |
| --- | ---------------------------------------------- | ------------------------------------------------------------------------ |
| 1   | QueuePage modo claro + job em processamento    | Cards inactivos com fundo cinzento ligeiro (#f1f5f9); ícone Film visível |
| 2   | HelpModal modo claro                           | Itens sidebar inactivos legíveis; tips labels e chevrons com contraste   |
| 3   | AssetDetailPage — hover sobre thumbnail        | Texto do path branco/90 sobre overlay escuro                             |
| 4   | AssetDetailPage — path abaixo do player        | Texto mais escuro em modo claro                                          |
| 5   | AssetDetailPage — tab Histórico, job activo    | Fases azul/verde/cinzento + barra de progresso percentual                |
| 6   | AssetDetailPage — tab Histórico, job concluído | Mini-pipeline todo verde com checkmarks                                  |
| 7   | Sidebar inferior                               | `v0.30.9-beta.1` (versão completa, não `V0.30.9-BETA.1`)                 |

- [ ] **Step 3: Actualizar SYNC-STATE.md**

Registar v0.31.0 como próxima versão com os 5 fixes.

- [ ] **Step 4: Commit final**

```bash
git add SYNC-STATE.md
git commit -m "docs(session): v0.31.0 -- 5 fixes UI modo claro, pipeline historico, versao sidebar"
```

---

## Self-Review

**Spec coverage:**

- ✅ QueuePage light mode: Task 2 steps 4-5
- ✅ HelpModal light mode: Task 3
- ✅ Preview URL overlay + path: Task 4 steps 3-4
- ✅ Pipeline histórico fases com progresso: Task 4 step 5
- ✅ Versão completa sidebar: Task 5

**Placeholder scan:** Sem TBD. Todo o código está completo.

**Correcções em relação ao spec:**

- Spec dizia `qc_pre` com underscore — código real usa `qc-pre` com hífen. Plano usa hífens.
- Spec propunha novas chaves i18n `pipeline.phase.*` — chaves já existem como `queue.phaseAnalyse`, `queue.phaseConvert`, `queue.phaseVerify`. Plano reutiliza as existentes.
- `PIPELINE_STEPS` em AssetDetailPage tem campo `short` que QueuePage não tem — `pipeline.ts` exporta com `short` para satisfazer ambos.

**Type consistency:** `getStepIndex` definido uma vez em `pipeline.ts`, importado em Task 2 (QueuePage) e Task 4 (AssetDetailPage) — consistente. `PIPELINE_STEPS` e `PIPELINE_PHASES` usam o mesmo array em ambas as páginas.
