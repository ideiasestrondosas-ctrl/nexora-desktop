# QA Runner Fixtures

This directory is reserved for QA Runner-specific fixtures.

The default media fixture is intentionally reused from:

```text
tests/fixtures/test-720p-5s.mp4
```

The runner copies fixtures into each run directory before using them. Original files must not be modified.
