# Dependencias
node_modules/
.pnp

# Build
dist/
build/
out/

# Rust (GRANDE - nunca commitar)
target/
src-tauri/target/

# Sidecar compilado
sidecar/dist/

# Binarios de media reais (grandes - descarregar com npm run download:binaries)
# Os ficheiros placeholder vazios SAO commitados para cargo check passar
src-tauri/binaries/*.exe
src-tauri/binaries/ffmpeg-*
src-tauri/binaries/ffprobe-*
# Manter os placeholders rastreados mesmo com os padroes acima
!src-tauri/binaries/ffmpeg-x86_64-pc-windows-msvc.exe
!src-tauri/binaries/ffprobe-x86_64-pc-windows-msvc.exe
!src-tauri/binaries/ffmpeg-aarch64-apple-darwin
!src-tauri/binaries/ffprobe-aarch64-apple-darwin
!src-tauri/binaries/ffmpeg-x86_64-apple-darwin
!src-tauri/binaries/ffprobe-x86_64-apple-darwin
!src-tauri/binaries/ffmpeg-x86_64-unknown-linux-gnu
!src-tauri/binaries/ffprobe-x86_64-unknown-linux-gnu
!src-tauri/binaries/ffmpeg-universal-apple-darwin
!src-tauri/binaries/ffprobe-universal-apple-darwin

# SQLite
*.db
*.db-shm
*.db-wal

# Ambiente
.env
.env.local
!.env.example

# Logs
*.log
logs/

# SO
.DS_Store
Thumbs.db
desktop.ini

# IDEs
.idea/
.vscode/settings.json
.vscode/launch.json
.vscode/*.code-workspace

# Claude Code — ficheiros de sessao locais (nao commitar)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/*.lock

# Antigravity IDE — configuracoes locais (manter rules.md)
.antigravity/settings.json

# Testes
coverage/
.tmp/
temp/
WixTools/

# Rust gerado (pode ser regenerado por cargo build)
src-tauri/gen/