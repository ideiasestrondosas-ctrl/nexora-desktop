# WIP - Nexora Desktop

**Ultima actualizacao:** 2026-06-03
**Tarefa activa:** Fix OAuth cloud GDrive Personal — sessao concluida

## Estado: CONCLUIDO — reiniciar com /clear para proxima tarefa

### Branch: main

### Commits desta sessao (por ordem cronologico)

1. `bffdb09` — Dropbox porta fixa 8475 + icon ? tema claro + GDrive help
2. `45a83f7` — Race condition oauth_token: useRef sincrono antes do toast
3. `0669f1b` — keyring v3 sem features = MockCredential (nao persiste); fix: windows-native + apple-native
4. `5187089` — CHECK constraint DB nao incluia dropbox/gdrive_personal
5. `ea2109e` — feedback visual verde ao copiar codigo GDrive Device Flow
6. `e72bfec` — bloquear Escape/clique fora do modal durante e apos OAuth
7. `4de65c4` — client_secret no refresh GDrive + modal scrollavel

### Estado por provider

| Provider | Auth | Estado |
|---|---|---|
| `gdrive` | Device Flow (codigo) | OK |
| `gdrive_personal` | PKCE (browser) | Perfil criado OK; browse a verificar |
| `dropbox` | PKCE (browser) | OK (WinCredential confirmado) |
| `sftp` | SSH | Nao testado end-to-end |

### Pendentes (proxima sessao)

1. Verificar browse GDrive Personal — fix refresh (client_secret) aplicado em 4de65c4; requer teste
2. Teste manual SFTP end-to-end
3. media-binaries.lock.json macOS — requer --write-lock em maquina macOS

### Notas para o proximo agente

- GDrive Personal requer: Client ID + Client Secret (GOCSPX-xxx) de credencial "Desktop app"
- Redirect URIs no Google Cloud Console: deixar VAZIO (aceita 127.0.0.1 com qualquer porta)
- Adicionar email do utilizador em "Utilizadores de teste" se app em modo Testing
- Dropbox: registar http://127.0.0.1:8475 exacto no App Console -> Settings -> Redirect URIs
