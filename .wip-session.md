# WIP - Nexora Desktop

**Ultima actualizacao:** 2026-06-03
**Tarefa activa:** Sessao 65 — Help text todos os providers cloud

## Estado: CONCLUIDO — reiniciar com /clear para proxima tarefa

### Branch: main

### Commits desta sessao

- Nenhum commit efectuado (alteracao pequena, aguarda commit pelo utilizador)

### Alteracao desta sessao

- `src/store/cloud.ts`: PROVIDER_HELP preenchido para FTP, SFTP, SMB, S3, iCloud
  (anteriormente null; agora com instrucoes passo-a-passo em PT)

### Estado por provider

| Provider | Auth | Estado |
|---|---|---|
| `gdrive` | Device Flow (codigo) | OK |
| `gdrive_personal` | PKCE (browser) | Browse verificado (logs OK) |
| `dropbox` | PKCE (browser) | OK (WinCredential confirmado) |
| `sftp` | SSH | Nao testado end-to-end |

### Pendentes (proxima sessao)

1. Teste manual SFTP end-to-end (1a ligacao / repeticao / mismatch MITM)
2. media-binaries.lock.json macOS — requer --write-lock em maquina macOS

### Notas para o proximo agente

- GDrive Personal: browse a funcionar (logs mostraram WinCredential + googleapis OK)
- As "crashes" da sessao foram reinicializacoes manuais do utilizador, nao panics Rust
- React StrictMode (dev) faz double-invoke de effects — normal ver 2 chamadas simultaneas
- Dropbox: registar http://127.0.0.1:8475 exacto no App Console -> Settings -> Redirect URIs
