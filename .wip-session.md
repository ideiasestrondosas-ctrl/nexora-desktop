# WIP - Nexora Desktop

**Ultima actualizacao:** 2026-06-02
**Tarefa activa:** Concluída — v0.32.0-beta.1 publicada + sync.ps1 v1.4.2

## Estado: RELEASE PUBLICADA ✅

v0.32.0-beta.1 publicada em https://github.com/ideiasestrondosas-ctrl/nexora-desktop/releases/tag/v0.32.0-beta.1

### Sessao actual (sessao 63)
Fixes aplicados e publicados:

1. **Tag em falta** — tag v0.32.0-beta.1 nunca existia no GitHub, Build nunca disparava.
   - Criada e publicada manualmente: `git tag -a v0.32.0-beta.1 && git push origin v0.32.0-beta.1`

2. **sync.ps1 v1.4.0** (`86ce431`) — opcao 4 "Ignorar versao" em Release mode agora:
   - Cria tag local se nao existir e publica via `--tags`
   - Watch mostra "aguardar tag" em vez de "em fila" para Build
   - Stall detection verifica se tag existe no GitHub antes de assumir Build OK

3. **media-binaries.lock.json** (`63807c4`) — checksums BtbN actualizados:
   - win32-x64: novo checksum (BtbN actualizou binario hoje 2026-06-02 15:24 UTC)
   - linux-x64 e linux-arm64: adicionados da fonte oficial checksums.sha256

4. **Retag** — tag movida para commit `c13414c` (main com todos os fixes)
5. **sync.ps1 v1.4.2** (`5860474`): Invoke-PublishDraft agora detecta Build runs em branches de tag (fix filtro `head_branch`)
6. **git credential** — `gh auth setup-git` configurado; git push funciona sem PAT
7. **Release body** actualizado manualmente com commits desta sessão (em futuras releases o sync.ps1 gera automaticamente)

## Pendente pos-release (do relatorio §8)
1. ~~Rotacionar GitHub PAT (R1)~~ — CONCLUÍDO: PAT `ghp_1xPrA1J9...` revogado; `.env` limpo; sync.ps1 usa `gh auth token` como fallback
2. media-binaries.lock.json macOS — requer `--write-lock` em maquina macOS
3. Teste manual SFTP end-to-end

## Branch
- dev @ 63807c4
- main @ c13414c
- tag v0.32.0-beta.1 -> c13414c
