# WIP - Nexora Desktop

**Ultima actualizacao:** 2026-06-03
**Tarefa activa:** Fix OAuth cloud — implementado, aguarda teste manual

## Estado: IMPLEMENTAÇÃO COMPLETA ✅ — AGUARDA TESTE MANUAL

### Branch: main

### O que foi feito nesta sessao

**Fix 1 — Icone ? invisivel no tema claro (CloudProfileModal.tsx)**
- Idle: `text-text-muted` → `text-text-secondary` (token semantico, visivel em ambos os temas)
- Painel ajuda: `text-blue-300` / `text-blue-200/80` → `text-text-primary` / `text-text-secondary`
  (as cores blue-XXX eram invisiveis em fundo branco porque o projecto usa .dark class, nao dark: Tailwind)

**Fix 2 — Dropbox redirect_uri invalido (commands/cloud.rs)**
- Dropbox exige match exacto do URI → porta dinamica causava "Invalid redirect_uri"
- Agora usa porta fixa 8475: `redirect_uri = "http://127.0.0.1:8475"`
- PROVIDER_HELP actualizado: utilizador regista exactamente `http://127.0.0.1:8475` no App Console

**Fix 3 — GDrive Personal instrucoes melhoradas (store/cloud.ts)**
- Aviso explícito: deve ser "Desktop app", NAO "Web application"
- Nota: para Desktop app, o campo redirect URIs pode ficar vazio (Google aceita 127.0.0.1 com qualquer porta)
- Formato do client_id: `123456789-abc123xyz.apps.googleusercontent.com`
- Passo sobre adicionar email como utilizador de teste (se app em modo teste)

### Providers e como configurar

| Provider | Auth | Client Secret? | Redirect URI a registar |
|---|---|---|---|
| `gdrive` | Device Flow (codigo) | Sim | N/A |
| `gdrive_personal` | PKCE (browser) | Nao | (deixar vazio — Google aceita qualquer porta 127.0.0.1) |
| `dropbox` | PKCE (browser) | Nao | http://127.0.0.1:8475 (exacto) |

### Proximos passos

1. `npm run tauri dev` e testar:
   - Dropbox → registar `http://127.0.0.1:8475` no App Console → "Conectar com Dropbox"
   - GDrive Pessoal → credencial "Desktop app" → deixar redirect URIs vazio → "Conectar com Google Drive"
2. Quando verificado → commit + `sync.ps1` opcao 3 para release se necessario

### Pendentes anteriores (nao bloqueantes)

1. media-binaries.lock.json macOS — requer --write-lock em maquina macOS
2. Teste manual SFTP end-to-end (testes T10-03/04/05 no guia beta)
