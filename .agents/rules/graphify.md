## graphify

Rules:

- Before architecture questions, check graphify-out/GRAPH_REPORT.md if it exists
- After modifying code, run `graphify update .` to keep graph current
- Use `graphify query "how does sidecar communicate with Tauri"` for IPC flow questions
- Use `graphify path "DropZone" "delivery-worker"` to trace full processing path
