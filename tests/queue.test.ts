import { describe, it } from 'vitest';

describe('NexoraSimpleQueue (Stateless Migration)', () => {
  it('is bypassed since sidecar is now completely stateless', () => {
    // NexoraSimpleQueue was deleted in v0.18.0 as the sidecar is now stateless.
  });
});
